import React from 'react'
import ReactDOM from 'react-dom/client'
import PathToGrad from './PathToGrad.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PathToGrad />
  </React.StrictMode>,
)