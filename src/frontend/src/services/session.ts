/**
 * Temporary development session.
 *
 * Authentication / role-based session management is outside
 * the current implementation scope.
 *
 * Replace these values with the authenticated user context
 * when FR-18 is implemented.
 */
export const CURRENT_STUDENT_ID = 'TEST001';

export const CURRENT_USER_ID = 'USER-TEST-001';